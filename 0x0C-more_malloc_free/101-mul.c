#include <stdio.h>
#include <stdlib.h>

/**
 * _realloc - reallocates a memory block using malloc and free (realloc)
 * @ptr: pointer
 * @num1_size: size of num1 pointer
 * @num2_size: size of num2 pointer
 * Return: new void pointer or Error if it fails
 */
